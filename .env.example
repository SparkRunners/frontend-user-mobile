# --- Runtime mode ---
# mock -> use scooter mock API, dev/prod -> real backend
APP_ENV=mock

# --- Simulation Mode (Feature Flag) ---
# Enable simulation for testing with 1000 scooters
# Set to 'true' to use Socket.IO real-time updates
# Set to 'false' to use REST API polling
USE_SIMULATION=false
SIMULATION_SOCKET_URL=http://localhost:3000

# --- Auth / OAuth service (copy from auth-server-service .env) ---
AUTH_API_BASE_URL=
FRONTEND_URL=sparkapp://app
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=
GOOGLE_TEST_CALLBACK_URL=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_REDIRECT_URI=

# --- Scooter mock service ---
# Default port per README is http://localhost:3000/api/v1
SCOOTER_API_BASE_URL=http://localhost:3000/api/v1
